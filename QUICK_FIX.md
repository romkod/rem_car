# ⚡ Швидке виправлення помилки PERMISSION_DENIED

## Проблема
```
PERMISSION_DENIED: Permission denied
```

## Швидке рішення (5 хвилин)

### 1. Відкрийте Firebase Console
👉 https://console.firebase.google.com/project/remcar-a23dc/database/remcar-a23dc-default-rtdb/rules

### 2. Скопіюйте ці правила:

```json
{
  "rules": {
    "cars": {
      ".read": true,
      ".write": true,
      "$carId": {
        ".read": true,
        ".write": true,
        "categories": {
          ".read": true,
          ".write": true
        }
      }
    },
    "test": {
      ".read": true,
      ".write": true
    }
  }
}
```

### 3. Вставте в поле редактора правил

### 4. Натисніть **"Publish"** (Опублікувати)

### 5. Оновіть сторінку тесту (F5)

### 6. Натисніть "Перевірити правила" знову

## ✅ Готово!

Якщо все правильно, ви побачите:
```
✅ Правила безпеки налаштовані правильно!
Запис та читання працюють коректно.
```

## Якщо не працює

1. Перевірте, чи правильний проект вибрано (remcar-a23dc)
2. Перевірте, чи натиснули "Publish"
3. Очистіть кеш браузера (Ctrl+Shift+Delete)
4. Спробуйте інший браузер
5. Дивіться детальну інструкцію: `FIREBASE_RULES_SETUP.md`

## Альтернатива: Тестовий режим

Якщо нічого не допомагає, встановіть правила для тестування:

```json
{
  "rules": {
    ".read": true,
    ".write": true
  }
}
```

⚠️ **Увага:** Це дозволяє всім читати та записувати дані. Тільки для тестування!


